language: python
python:
  - 3.6
before_install:
  - python --version
  - pip install -U pip
  - pip install redis
  - pip install codecov
install:  
  - pip install .
script:
  - coverage run tests/tests.py
notifications:
  email: false
after_success:
  - codecov
